# ================================
# 应用配置
# ================================
NODE_ENV=production
PORT=3000

# ================================
# 数据库配置（必须配置）- MySQL 8.4.7
# ================================
# 数据库主机地址（生产环境中通常是外部数据库服务器）
DB_HOST=your_database_host
# 数据库端口（MySQL 默认端口）
DB_PORT=3306
# 数据库用户名
DB_USERNAME=your_database_username
# 数据库密码
DB_PASSWORD=your_database_password
# 数据库名称
DB_NAME=your_database_name

# ================================
# JWT 配置（必须配置）
# ================================
# JWT 密钥（生产环境中必须使用强密钥）
JWT_SECRET=your_jwt_secret_key_here_should_be_very_long_and_secure_at_least_32_characters
# JWT 过期时间
JWT_EXPIRES_IN=7d

# ================================
# 定时任务配置
# ================================
# 时区设置
SCHEDULER_TIMEZONE=Asia/Shanghai

# ================================
# 日志配置
# ================================
# 日志级别：error, warn, info, debug
LOG_LEVEL=info

# ================================
# CORS 配置
# ================================
# 注意：当前配置允许所有来源访问（适用于 Electron 应用）
# 如需限制特定域名，请修改 src/main.ts 中的 CORS 配置
CORS_ORIGIN=*

# ================================
# API 文档配置
# ================================
SWAGGER_TITLE=诛仙世界多账号管理系统 API
SWAGGER_DESCRIPTION=用于管理诛仙世界游戏多个账号的进度跟踪系统
SWAGGER_VERSION=1.0.0
SWAGGER_PATH=api-docs

# ================================
# Docker 部署配置（可选）
# ================================
# Nginx 端口配置（如果使用 nginx profile）
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# ================================
# 生产环境额外配置
# ================================
# 数据库连接池配置
DB_MAX_CONNECTIONS=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=60000

# Redis 配置（如果需要缓存）
# REDIS_HOST=your_redis_host_here
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password_here

# 监控和健康检查
HEALTH_CHECK_TIMEOUT=5000